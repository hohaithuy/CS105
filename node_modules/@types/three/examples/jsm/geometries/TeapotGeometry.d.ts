import { BufferGeometry } from '../../../src/Three';

export class TeapotGeometry extends BufferGeometry {
    constructor(
        size?: number,
        segments?: number,
        bottom?: boolean,
        lid?: boolean,
        body?: boolean,
        fitLid?: boolean,
        blinn?: number,
    );
    parameters: {
        size: number;
        segments: number;
        bottom: number;
        lid: number;
        body: number;
        fitLid: number;
        blinn: number;
    };
}
